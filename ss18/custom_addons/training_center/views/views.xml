<odoo>
  <data>
    <record id="view_training_class_list" model="ir.ui.view">
      <field name="name">training.class.list</field>
      <field name="model">training.class</field>
      <field name="arch" type="xml">
        <list decoration-info="state=='draft'" decoration-muted="state=='closed'" decoration-success="state=='open'" decoration-bf="state=='open'">
          <field name="name"/>
          <field name="subject_id"/>
          <field name="teacher_id"/>
          <field name="start_date"/>
          <field name="end_date"/>
          <field name="state" widget="badge"/>
          <field name="total_revenue"/>
        </list>
      </field>
    </record>

    <record id="view_training_class_form" model="ir.ui.view">
      <field name="name">training.class.form</field>
      <field name="model">training.class</field>
      <field name="arch" type="xml">
        <form>
          <sheet>
            <div class="oe_title">
              <h1>
                <field name="name" placeholder="Ví dụ: Python K18"/>
              </h1>
            </div>
            <group>
              <group>
                <field name="subject_id"/>
                <field name="teacher_id"/>
              </group>
              <group>
                <field name="start_date"/>
                <field name="end_date"/>
              </group>
            </group>

            <notebook>
              <page string="Danh sách Sinh viên">
                <field name="student_ids">
                  <list>
                    <field name="name"/>
                    <field name="student_id"/>
                    <field name="email"/>
                  </list>
                </field>
              </page>

              <page string="Lịch trình học">
                <field name="session_ids">
                  <list editable="bottom">
                    <field name="name"/>
                    <field name="date"/>
                    <field name="duration" sum="Tổng thời lượng"/>
                  </list>
                </field>
              </page>
            </notebook>
          </sheet>
        </form>
      </field>
    </record>

    <record id="view_training_class_search" model="ir.ui.view">
      <field name="name">training.class.search</field>
      <field name="model">training.class</field>
      <field name="arch" type="xml">
        <search>
          <field name="name"/>
          <field name="subject_id"/>
          <field name="teacher_id"/>
          <group expand="1" string="Group By">
            <filter string="Môn học" name="grp_subject" context="{'group_by': 'subject_id'}"/>
            <filter string="Giảng viên" name="grp_teacher" context="{'group_by': 'teacher_id'}"/>
          </group>
        </search>
      </field>
    </record>

  </data>
</odoo>