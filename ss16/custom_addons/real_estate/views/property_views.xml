<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree View -->
    <record id="view_real_estate_property_tree" model="ir.ui.view">
        <field name="name">real.estate.property.tree</field>
        <field name="model">real.estate.property</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price" widget="monetary"/>
                <field name="selling_price" widget="monetary"/>
                <field name="date_availability" optional="hide"/>
                <field name="active" invisible="1"/>
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_real_estate_property_form" model="ir.ui.view">
        <field name="name">real.estate.property.form</field>
        <field name="model">real.estate.property</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object" class="oe_stat_button" icon="fa-archive">
                            <field name="active" widget="boolean_button" options="{'terminology': 'archive'}"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Tên bất động sản..."/>
                        </h1>
                    </div>
                    
                    <group string="I. THÔNG TIN CƠ BẢN">
                        <group>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                            <field name="expected_price"/>
                        </group>
                        <group>
                            <field name="selling_price"/>
                        </group>
                    </group>
                    
                    <group>
                        <field name="description" placeholder="Nhập mô tả chi tiết..."/>
                    </group>
                    
                    <group string="II. CHI TIẾT CĂN NHÀ">
                        <group>
                            <field name="bedrooms"/>
                            <field name="living_area"/>
                            <field name="facades"/>
                            <field name="total_area" readonly="1"/>
                        </group>
                        <group>
                            <field name="garage"/>
                            <field name="garden"/>
                            <field name="garden_area" attrs="{'invisible': [('garden', '=', False)]}"/>
                            <field name="garden_orientation" attrs="{'invisible': [('garden', '=', False)]}"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_real_estate_property_search" model="ir.ui.view">
        <field name="name">real.estate.property.search</field>
        <field name="model">real.estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="postcode"/>
                <separator/>
                <filter string="Có vườn" name="has_garden" domain="[('garden', '=', True)]"/>
                <filter string="Có gara" name="has_garage" domain="[('garage', '=', True)]"/>
                <separator/>
                <filter string="Đang bán" name="active" domain="[('active', '=', True)]"/>
                <group expand="0" string="Nhóm theo">
                    <filter string="Mã bưu điện" name="group_postcode" context="{'group_by': 'postcode'}"/>
                    <filter string="Số phòng ngủ" name="group_bedrooms" context="{'group_by': 'bedrooms'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_real_estate_property" model="ir.actions.act_window">
        <field name="name">Bất động sản</field>
        <field name="res_model">real.estate.property</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_active': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Tạo bất động sản đầu tiên!
            </p>
        </field>
    </record>

    <!-- Menu -->
    <menuitem id="menu_real_estate_root"
              name="Bất động sản"
              sequence="70"/>

    <menuitem id="menu_real_estate_property"
              name="Tài sản"
              parent="menu_real_estate_root"
              action="action_real_estate_property"
              sequence="10"/>
</odoo>
